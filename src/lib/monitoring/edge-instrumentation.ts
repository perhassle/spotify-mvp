/**
 * Edge runtime instrumentation for performance monitoring
 */

// Edge runtime has limited capabilities compared to Node.js
export function initEdgeMonitoring() {
  console.log('Edge instrumentation initialized');
}

// Initialize on import
initEdgeMonitoring();